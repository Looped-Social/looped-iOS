# Disabled Workflows

These files are backups and are intentionally disabled because they are not inside `.github/workflows/` with a `.yml`/`.yaml` extension.

To re-enable iOS CI:

```bash
mv .github/workflows-disabled/ios-tests.yml.md .github/workflows/ios-tests.yml
```
